clear
close all

load workspace_chol_Ufirst
vardec_Ufirst = vardec; 
ss_Ufirst     = ss(:,1);

load workspace_chol_Ulast
vardec_Ulast  = vardec;
ss_Ulast      = ss(:,end);

horz = linspace(0,20,20);

figure(1)
plot(horz,vardec_Ufirst(4,:),'linewidth',1.5)
hold on
plot(horz,vardec_Ulast(3,:),'linewidth',1.5)
legend()

figure(2)
plot(ss_Ufirst)
hold on
plot(ss_Ulast)

corr(ss_Ufirst,ss_Ulast)